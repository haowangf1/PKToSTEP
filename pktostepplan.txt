
step1:利用PK接口把从PK_BODY取出所有的Topo(类似PK_BODY_ask_topology接口)和几何(类似PK_FACE_ask_surf接口取几何)，把Topo和几何转换到AMCAXExchangeBase定义的中间数据格式，这个需要分析
PK中Topo的父子关系，并按AMCAXExchangeBase的规则转换，将PK转换到AXExchangeBase后，可以再用AXExchangeBase的接口转会PK_BODY并序列化到xt文件(可以用软件渲染)以检查正确性

step2:把AMCAXExchangeBase定义的中间数据格式写出成STEP文件，并用cad assistant打开以检查其正确性